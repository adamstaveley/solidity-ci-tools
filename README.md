## Solidity CI Tools

- linter: `solium`
- coverage: `solidity-coverage`

Setup:
```
npm install -g truffle
npm install
npm run lint
npm run coverage
```

Notes:
- `solidity-coverage` does not close testrpc upon exit